[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "st-guitar-harmonic-engine"
version = "0.1.0"
description = "Deterministic foundation for symbolic guitar harmonic analysis"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]
